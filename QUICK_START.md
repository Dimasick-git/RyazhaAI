# ⚡ Быстрый старт AI RYAHA

## 📋 Установка Node.js (если еще не установлен)

### Windows

1. **Скачай Node.js:**
   - Зайди на [nodejs.org](https://nodejs.org)
   - Скачай LTS версию (рекомендуется)
   - Запусти установщик

2. **Установка:**
   - Нажимай "Next" везде
   - ✅ Убедись что стоит галка "Add to PATH"
   - Дождись окончания установки

3. **Проверка:**
   ```powershell
   node --version
   npm --version
   ```
   
   Должны показаться версии (например: v20.10.0)

---

## 🚀 Запуск проекта

### 1. Открой PowerShell или CMD

```powershell
# Перейди в папку проекта
cd "c:\Users\dimas\OneDrive\Рабочий стол\AI RYAHA"
```

### 2. Установи зависимости

```powershell
npm install
```

Это займет 1-2 минуты. Скачается ~500MB зависимостей.

### 3. Запусти dev сервер

```powershell
npm run dev
```

### 4. Открой в браузере

Сайт будет доступен на:
- **http://localhost:3000**
- Или адрес, показанный в консоли

### 5. Наслаждайся! 🎉

AI RYAHA готов к использованию!

---

## 🔧 Дополнительная настройка

### Настройка Hugging Face API (для полноценного AI)

1. Получи бесплатный токен:
   - Зайди на [huggingface.co](https://huggingface.co)
   - Зарегистрируйся
   - Settings → Access Tokens → New token
   - Скопируй токен

2. Добавь в код:
   - Открой `src/services/api.js`
   - Найди строку с `// 'Authorization': 'Bearer YOUR_HF_TOKEN'`
   - Раскомментируй и вставь свой токен:
   ```javascript
   'Authorization': 'Bearer hf_xxxxxxxxxx'
   ```

**Без токена** сайт работает в демо-режиме с заготовленными ответами.

---

## 🎮 Сборка для Nintendo Switch

### Требования:
- [DevkitPro](https://devkitpro.org/wiki/Getting_Started)
- Make
- Nintendo Switch с кастомной прошивкой

### Сборка:

**Windows (с DevkitPro):**
```powershell
cd switch-app
make
```

**Результат:** `ai-ryaha.nro`

### Установка на Switch:
1. Скопируй `ai-ryaha.nro` на SD карту
2. Положи в `/switch/ai-ryaha/`
3. Запусти через Homebrew Menu

**⚠️ Важно:** Перед сборкой для Switch обнови URL в `switch-app/source/main.cpp` на адрес задеплоенного сайта!

---

## 📦 Сборка для продакшна

```powershell
npm run build
```

Создаст папку `dist/` с оптимизированными файлами для деплоя.

---

## 🌐 Деплой на хостинг

### Netlify (самый простой способ)

1. **Через веб-интерфейс:**
   - Зарегистрируйся на [netlify.com](https://netlify.com)
   - Drag & drop папку `dist/` на сайт
   - Готово! 🎉

2. **Через Git (рекомендуется):**
   ```powershell
   # Инициализация Git
   git init
   git add .
   git commit -m "Initial commit"
   
   # Загрузка на GitHub
   git remote add origin https://github.com/YOUR_USERNAME/ai-ryaha.git
   git push -u origin main
   ```
   
   Затем на Netlify:
   - New site from Git
   - Выбери репозиторий
   - Build: `npm run build`
   - Publish: `dist`
   - Deploy!

### Vercel

```powershell
# Установи Vercel CLI
npm i -g vercel

# Деплой
vercel

# Production деплой
vercel --prod
```

---

## 🔥 Быстрые команды

```powershell
# Разработка
npm run dev          # Запуск dev сервера

# Продакшн
npm run build        # Сборка для продакшна
npm run preview      # Превью prod версии

# Switch
cd switch-app
make                 # Сборка .nro
make clean          # Очистка build файлов
```

---

## 🎨 Структура проекта

```
AI RYAHA/
├── src/                    # Исходный код
│   ├── components/         # React компоненты
│   ├── services/          # API сервисы
│   ├── App.jsx            # Главный компонент
│   └── index.css          # Стили
├── switch-app/            # Nintendo Switch приложение
│   ├── source/            # C++ код
│   └── Makefile          # Сборка
├── public/               # Статические файлы
├── dist/                 # Build (создается автоматически)
└── node_modules/         # Зависимости (создается при npm install)
```

---

## 🐛 Частые проблемы

### ❌ "npm не является командой"
**Решение:** Установи Node.js с nodejs.org

### ❌ Ошибки при npm install
**Решение:** 
```powershell
# Очисти кэш
npm cache clean --force

# Удали node_modules
Remove-Item -Recurse -Force node_modules

# Установи заново
npm install
```

### ❌ Порт 3000 занят
**Решение:** Vite автоматически выберет другой порт (3001, 3002 и т.д.)

### ❌ Сайт не открывается
**Решение:** 
- Проверь что dev сервер запущен
- Попробуй http://127.0.0.1:3000
- Отключи антивирус/файрволл

### ❌ AI не отвечает
**Решение:**
- В демо-режиме работают заготовленные ответы
- Для полного AI добавь Hugging Face токен
- Проверь консоль браузера (F12) на ошибки

---

## 💡 Советы

1. **Первый запуск:**
   - Демо-режим работает сразу
   - API ключи можно добавить потом

2. **Разработка:**
   - Изменения применяются мгновенно (Hot Reload)
   - Открой консоль браузера (F12) для отладки

3. **Перед деплоем:**
   - Протестируй сборку: `npm run build && npm run preview`
   - Проверь что все работает

4. **Switch приложение:**
   - Сначала задеплой сайт
   - Потом обнови URL в коде Switch
   - Затем собери .nro

---

## 📚 Дополнительная документация

- `README.md` - Общая информация о проекте
- `API_SETUP.md` - Настройка API ключей
- `DEPLOYMENT.md` - Деплой на хостинг
- `switch-app/README.md` - Nintendo Switch приложение

---

## 🤝 Нужна помощь?

1. Проверь документацию выше
2. Посмотри Issues на GitHub
3. Задай вопрос в Discussions

---

**🥛 Готово!** Теперь ты знаешь как запустить AI RYAHA! 🚀

Enjoy coding! 💜
