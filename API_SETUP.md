# 🔑 Настройка бесплатных API для AI RYAHA

Подробное руководство по получению и настройке бесплатных API ключей

## 🤖 Hugging Face - AI модели (ОБЯЗАТЕЛЬНО)

**Стоимость:** БЕСПЛАТНО 💰  
**Лимиты:** Очень щедрые для бесплатного использования  
**Время получения:** 2 минуты

### Шаг 1: Регистрация

1. Зайди на [huggingface.co](https://huggingface.co)
2. Нажми "Sign Up" в правом верхнем углу
3. Зарегистрируйся через:
   - Email (рекомендуется)
   - GitHub
   - Google

### Шаг 2: Подтверждение email

1. Проверь почту
2. Нажми на ссылку подтверждения
3. Войди в аккаунт

### Шаг 3: Создание токена

1. Нажми на свой аватар → Settings
2. В левом меню выбери "Access Tokens"
3. Нажми "New token"
4. Настройки токена:
   - Name: `AI RYAHA`
   - Role: `read` (достаточно для inference)
5. Нажми "Generate token"
6. **ВАЖНО:** Скопируй токен сразу! Он больше не покажется

### Шаг 4: Добавление в проект

Открой `src/services/api.js` и найди:

```javascript
headers: {
  'Content-Type': 'application/json',
  // Раскомментируй и добавь свой ключ
  // 'Authorization': 'Bearer YOUR_HF_TOKEN'
}
```

Замени на:

```javascript
headers: {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer hf_xxxxxxxxxxxxxxxxxxxxxxxxxx'
}
```

### Бесплатные модели

AI RYAHA настроен на работу с этими моделями:

1. **Mistral-7B-Instruct** (основная)
   - Отличное качество
   - Быстрая
   - Хорошо понимает русский

2. **Llama-2-7b-chat** (альтернатива)
   - От Meta
   - Очень умная
   - Требует больше времени

3. **FLAN-T5-Large** (запасная)
   - Быстрая
   - Хороша для простых задач

### Лимиты бесплатного плана

- ✅ 30,000 символов/месяц через API
- ✅ Неограниченный Inference API (с задержками)
- ✅ Все публичные модели
- ⚠️ Может быть queue при высокой нагрузке

---

## 🌤️ OpenWeather - Погода (ОПЦИОНАЛЬНО)

**Стоимость:** БЕСПЛАТНО 💰  
**Лимиты:** 1000 запросов/день  
**Время получения:** 5 минут

### Шаг 1: Регистрация

1. Зайди на [openweathermap.org](https://openweathermap.org)
2. Нажми "Sign In" → "Create an Account"
3. Заполни форму:
   - Username
   - Email
   - Password
4. Подтверди email

### Шаг 2: Получение ключа

1. Войди в аккаунт
2. Перейди в "API keys"
3. Ключ создастся автоматически
4. Или создай новый: "Generate" → введи имя → "Generate"
5. Скопируй API ключ

### Шаг 3: Активация

⚠️ **ВАЖНО:** Ключ активируется в течение 2 часов после создания!

### Шаг 4: Добавление в проект

В `src/services/api.js` найди:

```javascript
const API_KEY = 'YOUR_OPENWEATHER_KEY'
```

Замени на:

```javascript
const API_KEY = 'abc123def456...' // Твой ключ
```

### Бесплатный план включает:

- ✅ Текущая погода
- ✅ Прогноз на 5 дней
- ✅ 1000 запросов/день
- ✅ Данные по всем городам мира

---

## 💬 Quotable - Цитаты (ОПЦИОНАЛЬНО)

**Стоимость:** БЕСПЛАТНО 💰  
**Лимиты:** НЕТ!  
**API ключ:** НЕ НУЖЕН! 🎉

Этот API работает сразу без регистрации:

```javascript
// Уже настроен в проекте!
export async function getRandomQuote() {
  const response = await axios.get('https://api.quotable.io/random')
  return response.data
}
```

---

## 🎨 Дополнительные бесплатные API

### NewsAPI - Новости

**URL:** [newsapi.org](https://newsapi.org)  
**Бесплатно:** 100 запросов/день  
**Ключ:** Нужна регистрация

```javascript
const NEWS_API_KEY = 'your_key_here'
const url = `https://newsapi.org/v2/top-headlines?country=ru&apiKey=${NEWS_API_KEY}`
```

### JSONPlaceholder - Тестовые данные

**URL:** [jsonplaceholder.typicode.com](https://jsonplaceholder.typicode.com)  
**Бесплатно:** Безлимит  
**Ключ:** Не нужен

```javascript
const response = await axios.get('https://jsonplaceholder.typicode.com/posts')
```

### CoinGecko - Криптовалюты

**URL:** [coingecko.com/api](https://www.coingecko.com/api)  
**Бесплатно:** 50 запросов/минуту  
**Ключ:** Не нужен

```javascript
const url = 'https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd'
```

---

## 🔒 Безопасность API ключей

### ❌ НЕ ДЕЛАЙ ТАК:

```javascript
// Плохо - ключ в коде
const API_KEY = 'sk-1234567890'
```

### ✅ ДЕЛАЙ ТАК:

**Для разработки:**

1. Создай `.env.local`:
```bash
VITE_HF_TOKEN=hf_xxxxxxxxxxxx
VITE_OPENWEATHER_KEY=abc123def456
```

2. Используй в коде:
```javascript
const HF_TOKEN = import.meta.env.VITE_HF_TOKEN
```

**Для продакшна (Netlify/Vercel):**

1. Netlify: Site settings → Environment variables
2. Vercel: Settings → Environment Variables
3. Добавь переменные без файла `.env`

### .gitignore

Убедись что в `.gitignore` есть:
```
.env
.env.local
.env.production
```

---

## 📊 Мониторинг использования

### Hugging Face

1. Войди на huggingface.co
2. Settings → Usage
3. Смотри статистику по токенам

### OpenWeather

1. Войди на openweathermap.org
2. API keys → Statistics
3. Смотри количество запросов

---

## ⚡ Оптимизация запросов

### Кэширование

```javascript
// Кэш для ответов
const cache = new Map()
const CACHE_TIME = 5 * 60 * 1000 // 5 минут

async function cachedRequest(key, fetchFn) {
  const cached = cache.get(key)
  if (cached && Date.now() - cached.time < CACHE_TIME) {
    return cached.data
  }
  
  const data = await fetchFn()
  cache.set(key, { data, time: Date.now() })
  return data
}
```

### Rate limiting

```javascript
// Ограничение частоты запросов
const rateLimiter = {
  lastCall: 0,
  minInterval: 1000, // 1 секунда между запросами
  
  async wait() {
    const now = Date.now()
    const timeSinceLastCall = now - this.lastCall
    if (timeSinceLastCall < this.minInterval) {
      await new Promise(r => setTimeout(r, this.minInterval - timeSinceLastCall))
    }
    this.lastCall = Date.now()
  }
}
```

---

## 🐛 Решение проблем

### Ошибка 401 (Unauthorized)
- ❌ Неправильный API ключ
- ✅ Проверь ключ, скопируй заново

### Ошибка 429 (Too Many Requests)
- ❌ Превышен лимит запросов
- ✅ Подожди или обнови план

### Ошибка 403 (Forbidden)
- ❌ API ключ не активирован
- ✅ Подожди 2 часа (OpenWeather)

### Нет ответа от API
- ❌ Интернет или CORS
- ✅ Проверь подключение и настройки

---

## 📚 Полезные ссылки

- [Hugging Face Docs](https://huggingface.co/docs)
- [OpenWeather API Docs](https://openweathermap.org/api)
- [Awesome Free APIs](https://github.com/public-apis/public-apis)

---

**🔑 Готово!** Теперь у тебя есть все ключи для AI RYAHA! 🚀
